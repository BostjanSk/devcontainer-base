services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace:cached # ← important: binds your project code
    command: sleep infinity # ← keeps container alive
    ports:
      - "3000:3000"

  # Example extra service (you can remove or keep)
  # postgres:
  #   image: postgres:16
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_PASSWORD: devpassword
  #     POSTGRES_USER: devuser
  #     POSTGRES_DB: myapp_dev
  #   ports:
  #     - "5433:5432"               # different port → doesn't conflict with local postgres
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
# volumes:
# postgres-data:
